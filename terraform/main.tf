# TerraMind — platform infrastructure layer.
# Provisions in-cluster platform services (ingress, metrics-server for HPA, and
# the monitoring/logging/vault namespaces) into the Kind cluster.
# Application workloads live in ../kubernetes (applied by kubectl / Jenkins).

terraform {
  required_version = ">= 1.5"
  required_providers {
    kubernetes = {
      source  = "hashicorp/kubernetes"
      version = "~> 2.31"
    }
    helm = {
      source  = "hashicorp/helm"
      version = "~> 2.17"
    }
  }
}

provider "kubernetes" {
  config_path    = var.kubeconfig
  config_context = var.kube_context
}

provider "helm" {
  kubernetes {
    config_path    = var.kubeconfig
    config_context = var.kube_context
  }
}
