# Point this at the FastAPI backend once it is running.
# When unset, the frontend runs fully on the built-in simulation engine
# (so it is demoable standalone, and degrades gracefully if the backend dies).
NEXT_PUBLIC_API_URL=
